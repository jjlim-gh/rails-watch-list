<div class="row container p-2 justify-content-center">
  <h1 class="col-6">All lists 📃</h1>

  <% @lists.each do |list| %>
    <div class="card col-6 m-2">
      <div class="d-flex justify-content-between">
        <h3 class="m-2"><%= list.name %></h3>
        <%= link_to "Delete", list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger m-2" %>
      </div>
      <%= link_to "See List", list_path(list), class: "btn btn-primary m-2" %>
    </div>
  <% end %>

  <%= link_to "Create New List", new_list_path, class: "btn btn-primary m-2 col-6" %>

</div>
